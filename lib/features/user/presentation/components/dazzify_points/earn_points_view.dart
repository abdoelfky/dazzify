import '../../../../../core/framework/export.dart';

class EarnPointsView extends StatelessWidget {
  const EarnPointsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: DText("EARN POINTS"),
      ),
    );
  }
}
